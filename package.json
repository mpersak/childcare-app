{
  "name": "childcare-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Childcare management: attendance, schedules, calendar, notes, hourly invoicing and financials.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "selftest": "tsc src/lib/selftest.ts --outDir .selftest --module commonjs --target ES2022 --moduleResolution node --skipLibCheck && node -e \"require(`fs`).writeFileSync(`.selftest/package.json`,`{\\\"type\\\":\\\"commonjs\\\"}`)\" && node .selftest/lib/selftest.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.9"
  }
}
